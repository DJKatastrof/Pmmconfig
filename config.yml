## This file is a template remove the .template to use the file

libraries:                                      # This is called out once within the config.yml file                                       
  #Movies:
    #metadata_path:
      #- file: config/metadata/movies/charts.yml
        #schedule: weekly(tuesday)
      #- file: config/metadata/movies/streaming.yml
        #schedule: weekly(tuesday)
  #TV:
    #metadata_path:
      #- file: config/metadata/series/streaming.yml
        #schedule: weekly(tuesday)
      #- file: config/metadata/series/charts.yml
        #schedule: weekly(tuesday)
  #TV (4K):
    #overlay_path:
      #- remove_overlays: true      # Set this to true to remove all overlays
      #- file: config/metadata/series4k/audio_codec.yml
        #schedule: daily
      #- git: PMM/overlays/resolution
        #schedule: daily                          # This is a file within the https://github.com/meisnate12/Plex-Meta-Manager-Configs Repository
settings:                                      
  cache: true
  cache_expiration: 60
  asset_directory: config/assets
  asset_folders: true
  asset_depth: 0
  create_asset_folders: false
  prioritize_assets: false
  dimensional_asset_rename: false
  download_url_assets: false
  show_missing_season_assets: false
  show_missing_episode_assets: false
  show_asset_not_needed: true
  sync_mode: append
  minimum_items: 1
  default_collection_order:
  delete_below_minimum: true
  delete_not_scheduled: false
  run_again_delay: 2
  missing_only_released: false
  only_filter_missing: false
  show_unmanaged: true
  show_filtered: false
  show_options: false
  show_missing: false
  show_missing_assets: true
  save_report: false
  tvdb_language: eng
  ignore_ids:
  ignore_imdb_ids:
  item_refresh_delay: 0
  playlist_sync_to_user: all
  playlist_report: false
  verify_ssl: true
  custom_repo:
  check_nightly: false
webhooks:                                       # Can be individually specified per library as well
  error:
  version:
  run_start:
  run_end:
  changes:
plex:                                           # Can be individually specified per library as well
  url: https://YOURSERVER@TAKEONE.take-one.world
  token: **TOKEN HERE**
  timeout: 60
  clean_bundles: true
  empty_trash: false
  optimize: false
tmdb:
  apikey: TMDB KEY HERE
  language: en
  cache_expiration: 60
  region:
trakt:
  client_id: 
  client_secret: 
  authorization:
    access_token: 
    token_type: 
    expires_in: 
    refresh_token: 
  pin:
